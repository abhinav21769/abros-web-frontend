// The product name shown before anyone signs in. Once signed in the UI is
// branded with the user's own company instead.
export const APP_NAME = import.meta.env.VITE_APP_NAME || "Abros Healthcare";
